To run the server, run "node server.js" using command prompt.

The messages can be sent to "ws://localhost:8080" using Postman web socket requests

  
(If you encounter any issues, make sure you have "node express" and "wss" installed.)
